# Labs

`labs/` 保存为了回答明确技术问题而需要独立依赖、可执行代码和重做命令的实验。实验可以不稳定，但必须如实说明
当前能证明什么、不能证明什么；保留价值来自可重做的问题和观察结果，而不是目录名称。

二级目录按问题领域组织，三级目录是独立模块。当前领域和模块以 [`modules.toml`](../modules.toml) 为准。

详细准入和迁移规则见 [仓库架构](../docs/project/architecture.md)。
