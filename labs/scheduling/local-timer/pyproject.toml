[project]
name = "local-timer-lab"
version = "0.1.0"
description = "基于堆和线程同步的本地定时器实验"
requires-python = ">=3.12,<3.15"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.3,<9"]

[tool.uv]
package = false
