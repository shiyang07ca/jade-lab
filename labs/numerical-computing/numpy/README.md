# NumPy Lab

需要 NumPy 运行时的数值计算实验，与无第三方运行时依赖的 Python 软件包分离。当前实验实现一个确定性的 item-to-item
协同过滤切片，明确约定零表示缺失评分，并验证矩阵形状、有限非负输入、相似度对称性和候选项过滤。

```sh
mise run check lab-numpy
```

该实现用于学习向量化边界和数值测试，不包含离线指标、时间切分或线上反馈，不能据此声称推荐质量。
