# B-Tree 实验

验证 B-Tree 的插入、节点分裂、查找和有序遍历。`order` 表示节点的最大子节点数，记录可以位于内部节点；B+ Tree 则
把记录保存在叶节点。当前实现使用不可变式插入返回新树，测试覆盖多种阶数和确定性输入。

当前范围是内存中的插入、查找和遍历。数据库索引所需的删除、页管理、持久化和并发机制属于其他实现范围。

进入本目录后运行：

```sh
uv run pytest
uv run ruff check .
```
