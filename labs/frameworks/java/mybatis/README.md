# MyBatis Lab

MyBatis、Mapper XML 与 MySQL 驱动的隔离实验。数据库地址和凭证只能通过本地配置提供，不进入版本库。

运行前设置以下环境变量：

- `LAB_MYSQL_URL`：JDBC URL，例如 `jdbc:mysql://127.0.0.1:3306/lab`
- `LAB_MYSQL_USERNAME`：数据库用户名
- `LAB_MYSQL_PASSWORD`：数据库密码
