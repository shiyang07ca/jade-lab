# Agent 指南

Jade Lab 保存算法题解、学习工作区、独立实验、工具和外部参考源码。内容分别位于 `algo/`、`learning/`、`labs/`、
`tools/` 和 `references/`，具体分类见 [`docs/project/architecture.md`](docs/project/architecture.md)。

## 阅读入口

1. 先读根目录 [`CONTEXT.md`](CONTEXT.md)，使用其中已经定义的领域名称。
2. 读取目标目录的 README、依赖文件、实现和测试，从当前代码确认行为与命令。
3. 新增、移动或删除内容时，读取 [`docs/project/architecture.md`](docs/project/architecture.md)。
4. 教学任务读取 [`learning/PROFILE.md`](learning/PROFILE.md) 和对应主题 README；目标、当前状态和下一步由主题 README 维护，按需查阅练习与真实学习记录。学习画像的修改由用户逐项确认。

## 修改流程

1. 运行目标项目 README 中的最窄检查，记录修改前的实际状态。
2. 修改实现及其直接测试和说明；依赖声明变化时同步更新适用的锁文件。
3. 新增、移动或删除项目时更新所属目录 README，并决定是否修改 `mise.toml` 的显式项目列表。
4. 把临时构建、调查输出和可重建文件写入 `.scratch/` 或语言工具的忽略目录。
5. 外部参考源码保持上游内容；版本更新通过父仓库 gitlink 记录。

同一事实由一个文件维护：工具版本和根任务以 `mise.toml`、`mise.lock` 为准，项目入口和命令以项目 README 为准，
目录分类和检查范围以架构文档为准，课程进度以对应主题目录为准。

## 文档语言

- 面向项目成员的说明、规格文档和架构决定默认使用中文。先写结论，再说明具体对象、动作、条件和依据。
- 使用 `CONTEXT.md` 中的中文主名称，在同一文档中保持术语一致。
- 外部规范第一次出现时写清中文含义并保留准确原名；标识符、API、协议、命令、路径、配置键、状态值、错误消息和
  引用原文保持原样。
- 使用具体名词和动词，直接说明文件、项目、状态和动作。
- `CONTEXT.md` 保存跨任务长期使用且容易混淆的领域名称；参数、验收条件、动态状态和实现步骤由对应项目文档维护。

## Git 提交

后续提交信息使用提交时的本地时间，格式为 `YYYY-MM-DD HH:mm`，例如 `2026-09-13 23:19`。完整提交信息只包含该时间戳。

## 验证与交付

先运行受影响项目的最窄检查，再运行对应分区任务：`check:algo`、`check:learning` 或 `check:labs`。准备提交代码、
配置、依赖或目录结构变化时运行：

```sh
mise run check
git diff --check
```

纯文档修改运行 `git diff --check`，并检查改动中的相对链接、路径和命令。
