# 第 11 课历史学习记录

当时完成了 curl 与 jq 的即时自检。没有“curl 永远加某选项”或“jq 输出永远加 `-r`”的通用规则；应按接口定义
HTTP 失败、期限、响应体、JSON 类型和空值。`jq -e` 对 false/null、无结果和解析错误有不同状态。

恢复检查：用本地夹具验证有效 JSON、字段缺失、错误类型、HTTP 404 和超时，并说明每个状态如何映射到调用者。
