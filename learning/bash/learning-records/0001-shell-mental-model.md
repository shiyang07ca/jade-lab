# 建立了 Shell 心智模型

用户理解了 Shell 的本质——命令启动器（fork + exec）、PATH 搜索机制、退出码（0=成功/非0=失败）、以及 stdin/stdout/stderr 三条数据流。通过了第 1 课的自检题目。

**Implications**：可以引入管道和重定向（第 3 课），因为用户已理解数据流的概念。后续涉及 `if`、`&&`、`set -e` 时可以直接引用退出码概念。
