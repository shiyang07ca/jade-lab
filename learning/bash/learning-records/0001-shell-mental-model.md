# 第 1 课历史学习记录

当时完成了 Shell 执行模型的即时自检，能识别命令查找、退出状态和标准流。该记录不证明当前掌握，也不把所有命令
执行简化为 fork + exec：函数和 builtin 可在当前 Shell 执行，外部命令才适用简化的进程启动模型。

恢复检查：预测 `type cd`、`type printf`、`type grep` 和一个不存在命令的输出与状态，再运行核对。
