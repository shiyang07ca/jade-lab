# 掌握了核心文件系统操作

用户掌握了 ls 高级选项（-l, -a, -h, -t）、目录导航（pwd, cd -, pushd/popd 概念）、文件查看（less, head, tail -f）、创建/删除（mkdir -p, touch, rm -r）、复制移动（cp, mv）、权限基础（ls -l 解读、chmod +x 和数字模式）、以及 file 命令。通过了 4 道自检题。

**Implications**：用户可以开始学习管道和重定向（第 3 课），这是将简单命令组合成强大操作的关键。后续涉及部署脚本中的日志重定向（`>> log 2>&1`）时已有基础。
