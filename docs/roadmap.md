# Roadmap

## 当前阶段

- 使用 `modules.toml` 和根命令维护模块索引、环境诊断和默认检查。
- 使用 `learning/` 管理长期课程和已证明的学习进度，不再把课程保存在 `.scratch/`。
- 补齐现有 lab 的目标说明和最小验证。
- 按实际调用方式把成熟实验整理为稳定软件包或可执行工具。
- 为稳定模块逐步增加与风险相称的自动检查，并删除无用途的占位实现。

## 后续阶段

- 根据实际运行时间决定是否把语言或高成本模块从默认 CI 拆成独立任务；外部服务实验不进入默认任务。
- 建立系统设计案例、性能实验和结果复现规范。
