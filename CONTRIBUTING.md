# 修改与验证

一次变更只处理一个能够独立验证的主题。修改前先运行目标模块的最窄检查；完成后运行相同检查并扩大到该模块的
完整检查。依赖声明与锁文件同步更新，目录迁移、行为修改和依赖升级尽量分开。

## 内容去向

- 学习使命、课程、资料和已经由表现证明的学习结果进入 `learning/`。
- 由其他代码导入、链接或声明为依赖的稳定模块进入 `packages/`。
- OJ 尝试进入 `problems/`；这里允许未完成实现。
- 围绕明确问题且需要独立环境的验证进入 `labs/`。
- 由人、CI 或定时任务直接启动、完成一个命名的重复工作流程的程序进入 `tools/`。
- 需要长期重复阅读的第三方源码以固定 submodule 进入 `references/`。
- 一次性验证、机器状态、缓存和完整日志进入 `.scratch/`。

不能证明用途或行为的占位实现直接删除，由 Git 历史承担恢复职责。课程中产生的代码仍按代码本身的调用方式归类，
不复制到 `learning/`。`packages/` 与 `tools/` 的准入条件见
[`docs/project/architecture.md`](docs/project/architecture.md)，各目录 README 只保存模块清单和本目录特有说明。

## 检查

修改模块：

```sh
mise run check <module-id>
```

修改根目录规则、模块索引、公共文档或目录结构：

```sh
mise run policy
mise run check
```

新增或迁移模块时同步更新 `modules.toml`。`stable` 模块必须提供无需外部服务的自动检查；需要数据库、容器、
公网或特定平台的实验不得进入默认检查，其 README 必须说明准备步骤、风险和当前限制。

软件包测试必须从公开接口调用，并验证能生成该语言的依赖产物。工具测试必须验证参数、stdout/stderr、退出码和
主要状态变更；涉及真实远程系统的写操作不属于默认检查。

不得提交凭证、虚拟环境、构建产物、编辑器本机状态、完整运行日志或个人临时文件。submodule 中不记录个人笔记；
研究结论写入 `docs/open-source/`。
